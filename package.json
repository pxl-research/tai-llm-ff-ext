{
  "name": "ff-llm-ext",
  "version": "2.0.0",
  "description": "Firefox extension that autofills web forms using an LLM via OpenRouter.",
  "private": true,
  "license": "PolyForm-Noncommercial-1.0.0",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage --coverage.include=\"scripts/**\" --coverage.include=\"popup/**\""
  },
  "engines": {
    "node": "^22.22.2 || ^24.15.0 || >=26.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^30.0.1",
    "vitest": "^4.1.10"
  }
}
